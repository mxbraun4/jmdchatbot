# Replace example.com with your domain
example.com {
    encode gzip

    # Route /query to the FastAPI backend running on the host or another machine
    reverse_proxy /query* http://127.0.0.1:8000

    # Everything else to Next.js app container
    reverse_proxy / http://127.0.0.1:3000
}
